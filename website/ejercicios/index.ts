console.log('Hello', 'TypeScript')

function add (a: number, b: number) {
    return a + b;
};

const suma = add(2,3);

let numerador = 42;

enum Color {
    Rojo = 'Rojo',
    Verde = 'Verde',
    Morado = 'Morado',
};

let colorFavorite = Color.Rojo

console.log(`Mi color favorito es el ${colorFavorite}`)

